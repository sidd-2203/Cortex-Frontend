@AGENTS.md

## Design system

Before writing or changing any UI — a new component, a restyle, a new page,
or anything touching `globals.css`, `composer.tsx`, `chat-sidebar.tsx`, or
`chat-workspace.tsx` — load the skill: `design-system`.
